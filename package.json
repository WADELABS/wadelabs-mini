{
  "name": "wadelabs-mini",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://WADELABS.github.io/wadelabs-mini",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^7.1.9"
  }
}
